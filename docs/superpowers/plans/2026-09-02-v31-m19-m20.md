# v3.1 Uygulama Planı — M19-M20

Spec: `docs/superpowers/specs/2026-09-02-disport-v31-tasarim.md`
Döngü: kod → test → `flutter analyze` temiz + `flutter test` yeşil →
commit (conventional, İngilizce). Görev testsiz bitmez.

Tuzak hatırlatmaları: widget testinde Drift yok (provider override);
kabuktan görünür her yeni Drift akışı `test/app/app_test.dart`
override listesine eklenir; ARB değişince `flutter gen-l10n`;
@riverpod eklenince `dart run build_runner build`; import sırası
alfabetik.

---

## M19 — Şema v16 + gün ekranı gerçeklik blokları + BMI

### T19.0 Ana Sayfa mockup uyumu (kullanıcı bildirimi, 2026-09-02)

Cihazda görülen ekran mockup'ın (B1/B2) uzağında. Kapatılacak farklar:

- **Kahraman panelde değil.** Kahraman + metrik şeridi `AppPanel`'e
  girer. Bugün şeridi: Protein · Yakılan · **Su** · **İlaç**; bugün
  değilse: Protein · Yakılan · **Kilo** (mockup B2). Program/kural
  sayaçları şeritten çıkar (akış sayacı zaten var).
- **Çift liste kalkar.** `_Spine`'ın slot listesi silinir — SIRADA
  kartı + GÜNÜN AKIŞI kalır (mockup'ta ikinci liste yok). Slot ekleme
  "+" akış başlığının sağına taşınır.
- **Hafta noktaları Ana Sayfa'dan çıkar** (mockup'ta yok).
- **Öğün satırı davranışı**: yapıldı = o öğüne kayıt girilmiş
  ("Kahvaltı — 486 kcal"); girilmemişse "+ GİR" rozeti ve dokunuş
  besin seçiciyi açar (mockup caption'ı: "öğün → seçici"). Kutucuk
  işaretleme davranışı öğünlerde kalkar.
- **Başlık günün adı** ("Pazartesi"), üst satır tarih · hafta;
  geçmişte üst satır amber "GEÇMİŞ GÜN · …".

Testler: akış modeli (öğün done = kayıt var), ekran (şerit içerikleri
bugün/geçmiş, "+ GİR" rozeti), app_test override listesi güncel.

Commit: `fix: align home screen with v3 mockup`

### T19.1 Şema v16 + veri katmanı

- `daily_logs`: `bedTime` Text?, `wakeTimeActual` Text?, `napMinutes`
  Int?, `moodScore` Int?, `symptoms` Text `''`, `stressedDay` Bool
  `false`, `skippedMealsJson` Text `'{}'`.
- `workout_sessions`: `rpe` Int?, `painNote` Text `''`.
- `medical_facts`: `factDate` Text?.
- `schemaVersion` 16, tek `if (from < 16)` bloğu.
- `TodayRepository`: uyku alanları + his alanları + atlama JSON'u için
  oku/yaz metotları; `DailyLogView`'a alanlar. Uyku süresi türetme saf
  fonksiyon: `domain/sleep_duration.dart` (gece yarısı aşımı +
  kestirme; "son yazan kazanır" kuralının repository yazımları).
- Testler: migration (v15 açılıp v16'ya çıkar), repository oku/yaz,
  `sleep_duration` saf testleri (23:45→06:11, 01:00→08:00, kestirme).

Commit: `feat: schema v16 - daily reality columns`

### T19.2 Uyku bloğu (gün ekranı)

- `measurement_inputs.dart`'taki uyku alanı yerine `SleepBlock`:
  yatış/kalkış saat seçici, kestirme dk, yalnız-süre alanı, türetilmiş
  süre satırı. Kapalı/açık tek satır davranışı.
- Yazım: saatler → türetilen süre `body_metrics.sleepHours`'a;
  yalnız-süre → saatleri temizle; saat silme → o günün türetilmiş
  `sleepHours` kaydını sil.
- `sleepHours` tanımı silinmiş olsa da blok görünür.
- Widget testleri provider override ile; app_test'e yeni akış
  override'ları. Gün kuralı testleri: gelecek günde blok gösterilmez,
  geçmiş güne girilebilir (spec §2.3).

Commit: `feat: daily sleep block with bed/wake/nap times`

### T19.3 His + belirti + stres bloğu

- Gün ekranında not alanının üstünde `MoodBlock`: 5 yüz (Lucide,
  seçili dolu, semantics etiketli), belirti metin alanı, "yoğun gündü"
  kutucuğu. Boşken tek satır.
- Repository yazımları T19.1'den; provider + widget testleri.

Commit: `feat: daily mood, symptoms and stress entry`

### T19.4 Adım ölçümü

- `MetricKinds.steps` + `labels` + `_dailyKinds` + `_wholeNumberKinds`;
  bootstrap tohumu (silinmişse geri gelmez — mevcut kalıp).
- Gün ekranı ölçüm girişlerinde görünür; tohum testi + giriş testi.

Commit: `feat: built-in daily steps metric`

### T19.5 Öğün atlama nedeni (Diyet → Günlük)

- `today`, atlama alanı için application katmanında sağlayıcı açar
  (repository metodu T19.1'de); `nutrition` **today'in application
  sağlayıcısını** çağırır, repository'yi doğrudan import etmez. Öğün
  kaydı eklenince atlama silinir (nutrition'ın ekleme akışında).
- Plan-gerçek satırına "atlandı" eylemi: çipler (mesai · iştahsızlık ·
  dışarıdaydım · diğer) + serbest alan; atlanmış satır soluk +
  alt yazı.
- ARB anahtarları; testler.

Commit: `feat: meal skip reasons in diet daily view`

### T19.6 BMI görünürlüğü

- `_BmiRow`: kilo sırası (ölçüm → profil `currentWeightKg`); eksik boy/
  kilo yönlendiren satır (dokunuş Profil'i açar); ikisi de yoksa gizli.
- Onboarding sihirbazının boy/kilo adımına canlı BMI satırı
  (değer + sınıf rozeti + bağlam cümlesi).
- Testler: üç veri durumu + sihirbaz canlı satırı.

Commit: `feat: visible BMI with fallbacks and onboarding preview`

---

## M20 — RPE, teşhis, context.md v2.1, süpürme

### T20.1 Seans sonu RPE + ağrı notu

- Antrenman ekranı: tüm setler bitince tamamlama alanında 1-10 çip
  dizisi + ağrı notu alanı; `workout_sessions.rpe/painNote` yazımı.
- Planlanan-yapılan ekranından geçmiş seansa giriş/düzeltme.
- Testler: yazım + geçmiş düzeltme + widget.

Commit: `feat: session RPE and pain note`

### T20.2 Tarihli teşhis

- `MedicalFactKind.diagnosis`; medikal ekranda teşhis bölümü (etiket,
  tarih, not; öneri çipleri aynı havuz). Aynı `conditionId`'li
  condition varsa dönüştürme (kind=diagnosis + tarih), ikinci satır
  açılmaz.
- `restriction_match` + check-up motoru diagnosis'i condition gibi
  okur (kimlikliyse).
- Testler: dönüştürme kuralı, motor okumaları, ekran.

Commit: `feat: dated diagnosis records`

### T20.3 context.md v2.1

- `LogSource` gün dökümü: uyku (saatler ya da süre), his, belirti,
  stres, atlanan öğünler; yeni `sessions({lastDays})` dökümü.
- `MedicalFactDump`: `diagnosis` + `factDate`; §3'e "### Tanılar".
- §7 gün satırı + seans satırları; §9'a RPE cümlesi. Veri yoksa alan
  yazılmaz.
- "Yedi bölümlü" bayat yorum düzeltilir.
- Adaptörler ilgili feature'ların `application/`ında güncellenir.
- Testler: builder çıktı (golden benzeri metin), port adaptörleri.

Commit: `feat: context.md v2.1 with daily reality data`

### T20.4 Süpürme + dokümantasyon

- Kullanılmayan ARB taraması; ölü kod; `hardcoded_text_test` yeşil.
- CLAUDE.md: v3.1 satırları (şema v16, yeni bloklar, context v2.1).
- Tam süit + analiz.

Commit: `docs: record v3.1 daily reality layer`

---

## Riskler

- **Uyku bloğu ile `sleepHours` çift yazımı** — "son yazan kazanır"
  kuralı repository'de tek yerde uygulanmalı; iki ekran ayrı yazarsa
  yarış doğar.
- **Sihirbazda BMI** onboarding testlerini kırabilir (yeni satır bulucu
  değiştirir) — bulucular anahtara bağlanır.
- **app_test override listesi**: gün ekranına eklenen her yeni akış
  kabuk testini asar; her görevde kontrol.
