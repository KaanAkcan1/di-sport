# v3 Uygulama Planı — M13–M18

**Spec:** `docs/superpowers/specs/2026-09-01-disport-v3-tasarim.md` (review'lu, 07a9680)
**Döngü:** TDD değil — **kod → testler → `flutter analyze` temiz + `flutter test` yeşil → commit** (conventional, İngilizce gövde).
**Uygulayıcı:** tek ajan, subagent yok (yalnız review/danışma serbest).
**Sıra bağlayıcı:** M13 → M14 → M15 → M16 → M17 → M18. Her milestone sonunda tam süit koşulur.

Ortak kurallar (CLAUDE.md bağlayıcı): feature-first, `core`→feature yasağı,
port/adapter, SyncColumns, ekranlar Stream okur, widget testinde Drift yok
(provider override), Riverpod aile argümanı `String`, metinler ARB'ye,
`TurkishText.upper`. Tuzak listesi geçerli (sqlite3.dll kilidi, engine
version, `flutter gen-l10n`).

---

## M13 — Gezinme iskeleti + görsel dil r2

Amaç: beş yeni sekme, segment kontrol, panel/ikon bileşenleri, ekran
taşımaları. **Bu milestone'da yeni özellik yok** — mevcut ekranlar yeni
yerlerine taşınır ve derleme + testler yeşil kalır.

### T13.1 Palet + bileşenler
- `app_palette.dart`: alan renkleri (`sport #56B4E9`, `health #CC79A7`,
  `med #B39DDB`, `energy #E69F00`) + `sportOnLight #2E7DAB`,
  `healthOnLight #A85B86` koyulaştırılmış varyantlar (3:1 fildişi üstünde —
  hesapla doğrula, gerekirse koyulaştır).
- `app_semantic_colors.dart`: `area(diet|sport|health|med|energy)` erişimi —
  tema moduna göre normal/OnLight döner.
- `core/widgets`: `AppPanel` (normal/accent/warn), `AppSegmented`,
  `AppIconTile` (40dp, alan renkli), `AppAccentRow` (sol vurgu çizgisi).
- `contrast_test.dart`: yeni renkler iki modda.
- `pubspec`: `phosphor_flutter` ekle (sürümü pub'dan güncel al; elle
  pubspec düzenle — `flutter pub add` `^` tuzağı).
- Commit: `feat: area colors, panel/segment/tile components, phosphor icons`

### T13.2 Kabuk
- `app.dart` `_Shell`: 5 sekme = AnaSayfa(TodayScreen) · Diyet(DietShell) ·
  Spor(SportShell) · Sağlık(HealthShell) · Daha(MoreScreen). Shell'ler
  `AppSegmented` + `IndexedStack` (alt sekmeler de canlı kalır).
  - `DietShell`: GÜNLÜK(**geçici olarak mevcut `DayMealsCard` monte edilir** —
    öğün kaydı M13–M15 arasında işlevsiz kalmasın; T15.1 plan/gerçekleşen
    görünümüyle değiştirir) · BESİNLER(iskelet) ·
    GEÇMİŞ(`CalorieWeekChart` + gün dökümü taşınır)
  - `SportShell`: PLAN(`PlanScreen` gövdesi) · ANTRENMAN(workout geçmişi
    iskeleti; aktif seans rotası aynen) · KATALOG(`CatalogScreen`)
  - `HealthShell`: TAHLİL(`HealthScreen`) · ÖLÇÜM(`ProgressScreen` gövdesi) ·
    İLAÇ(takviye ekranı Ayarlar'dan taşınır)
  - `MoreScreen`: §8 dizini — mevcut ekranlara satırlar (profil, medikal
    placeholder M14'e, ekipman, günlük düzen, yasaklı placeholder M15'e,
    kurallar, bildirim, görünüm+dil, yedek).
- Sekme ikonları phosphor duotone; etiketler ARB (`tabHome`, `tabDiet`…).
- `reminder_planner.dart` tabIndex eşlemesi spec §1.2 tablosuna göre;
  haritaya **`meal` anahtarı eklenir** (mevcutta yok — öğün bildirimi
  aksi hâlde varsayılan sekmeye düşer); `ReminderPayloads` testi
  güncellenir.
- MoreScreen yedek satırı: son yedek 4+ gün eskiyse amber uyarı (§8).
- **Commit bölünmesi:** (a) shell + taşımalar + test göçü,
  (b) MoreScreen + reminder eşlemesi — shell derlenir derlenmez taşınan
  testler koşulabilsin.
- Başlık kuralı: her shell kendi `head` düzenini kurar (sekme adı tekrarı
  yok; Ana Sayfa'da gün adı + ‹/takvim/›).
- Testler: kabuk testi 5 sekme + IndexedStack; taşınan ekranların mevcut
  testleri yeni konumlarıyla güncellenir (import/pump hedefi değişir,
  davranış değişmez).
- Commit: `feat: five-tab shell with segmented sub-tabs`

### T13.3 Ana Sayfa sadeleşmesi
- `TodayScreen` üç bölüme iner: kahraman panel (kalan kcal + strip:
  protein/yakılan/su/ilaç) · SIRADA panel · GÜNÜN AKIŞI (ilk 5 satır +
  "tamamı"). Öğün listesi/takviye kartı/ölçüm/kurallar/not **akış
  satırlarına** dönüşür: satır dokunuşu ilgili işlemi açar (öğün→Diyet
  GÜNLÜK ilgili grup, tartı→ölçüm girişi sheet, ilaç→işaretleme sheet,
  not→not sheet). Kurallar + not "tamamı" görünümünde.
- Akış birleşik model: `day_flow.dart` (saf) — plan slotları + ilaç dozları
  + tartı + su tek zaman çizgisinde; test saf katmanda.
- Su metriği M13'te mevcut `waterTargetMet` kutucuğundan okunur ("✓/—");
  T15.1 ml miktarına geçirir.
- Geçmiş/gelecek kuralları aynen (M10).
- **Test göçü açık madde:** `today_screen_test`, `today_screen_ink_test`,
  `day_screen_test` beş kartı doğrudan pompalıyor — üçü akış-satırı
  modeline yeniden yazılır; akış satırı → sheet yönlendirme testleri
  eklenir. Bu görev davranış değiştirir, "konum güncellemesi" değildir.
- Commit: `feat: three-section home with unified day flow`

---

## M14 — Şema v15 + onboarding + medikal + sporlar + öğün davranışı

### T14.1 Şema v15 (tek blok)
- Tablolar: `medical_facts(kind,label,note,conditionId?)`,
  `meal_behaviors(mealKind,time?,behavior,fixedNote?,fixedItemsJson?)`,
  `favorite_sports(activityId,note)`, `plan_meal_items(planSlotId,foodId,
  quantity,portionId?)` — hepsi SyncColumns.
- Sütunlar: `supplements.kind` (default `supplement`),
  `daily_logs.waterMl?`.
- Veri göçü: `equipment_items`e `chair`,`step` satırları `atHome=1,atGym=0`
  (yalnız v15 göçünde; taze kurulum tohumu işaretsiz).
- `EquipmentKind`: `chair`, `step` değerleri; `equipment_labels` +
  ARB anahtarları; katalog override `chair_squat`→chair, `step_up`→step;
  boru hattı çalıştır, tohum sürümü artır.
- Migration testi v14→v15; seed testler.
- Commit: `feat: schema v15 - medical, meal behaviors, favorite sports, plan meal items`

### T14.2 Onboarding akışı
- `onboarding/`: hoş geldin ekranı + 2 adımlı sihirbaz (kimlik: ad/soyad/
  doğum tarihi/cinsiyet; ölçü: boy/kilo/hedef — kilo `body_metrics`e).
  `profile_entries` yeni anahtarlar (`firstName`,`lastName`,`birthDate`,
  `gender`); eski `wakeTime` vb. onboarding'den çıkar (Günlük Düzen'e).
- Doğum günü: `todayIso == birthDate(ay-gün)` ise Ana Sayfa eyebrow'da
  kutlama metni.
- Kurulum kartları paneli: `setup_progress.dart` (saf, 4 girdi —
  **1. sihirbaz tamam:** profil anahtarları dolu; 2. ekipman: envanterde
  işaret var ya da geçildi; 3. medikal: kayıt var ya da geçildi;
  4. düzen: kalkış saati var ya da geçildi; geçilme `profile_entries`
  `setup.skipped.X`). 4/4'te panel kalkar. Ana Sayfa ilk-açılış hâli:
  kahraman yerine panel.
- Sihirbaz spec §3.1: hoş geldin + 2 veri adımı, ilerleme 3 nokta
  (3. nokta bitiş/geçiş) — bitiş ekranı düşmez.
- Commit: `feat: welcome wizard and setup cards`

### T14.3 Medikal feature
- `features/medical/`: domain (kind enum + condition catalog sabiti),
  data (repository), presentation (ekran: çip bölümleri + ilaç aynası +
  gizlilik notu). İlaç giriş sayfası `supplements` feature'ında genişler
  (tür seçimi) ve iki yerden açılır.
- Commit: `feat: medical facts screen and prescription/supplement split`

### T14.4 Ekipman + sporlar ekranı
- Üç segment; ev eşyası bölümü; "Etkisi" paneli (`canPerform` sayımı);
  satırlarda "N hareket açar" (katalogdan sayım). SEVDİĞİN SPORLAR:
  aktivite çok seçim + not; `favorite_sports` repository.
- Commit: `feat: equipment screen with impact counts and favorite sports`

### T14.5 Günlük Düzen genişlemesi
- Öğün saatleri + davranış listesi (`meal_behaviors` repository + editör
  sheet: saat, davranış segmenti, fixed not + isteğe bağlı besin bağlama —
  besin seçici yeniden kullanılır). Alarm penceresi öğün saatlerini buradan
  okur (`planned` davranışlılar).
- Commit: `feat: meal times and behaviors in daily rhythm`

---

## M15 — Diyet

### T15.0 plan_meal_items yazım yolu
- `PlanImporter` genişler: AI JSON'daki öğün `items` dizisi ayrıştırılır
  ve `plan_meal_items`e yazılır (fonksiyon imzasına insert callback);
  `plan_json.dart` slot modeli `items` alanı kazanır; plan editörü slot
  sheet'ine öğün kalemi ekleme (besin seçici yeniden kullanım) girer —
  elle kurulan plan da kalem taşıyabilsin. Testler: importer items yazar,
  itemssız slot eski davranış, editör kalem ekler/siler.
- Commit: `feat: plan meal items - import and editor write path`

### T15.1 GÜNLÜK
- Plan/gerçekleşen birleşimi: grup başına plan kalemleri
  (`plan_meal_items` + slot etiketi) soluk satır; `meal_entries` dolu.
  "Plandaki gibi yedim" (items varsa) / "Her zamanki" (fixed + bağlı) —
  snapshot o anki besin değerinden. `PLANA UYGUN` rozeti ±%15.
- Su: `waterMl` bardak satırı (+250 ml) hem burada hem Ana Sayfa şeridinde;
  su kuralı kablolaması spec §5.1 (kutucuk→miktar, `waterTargetMet`
  senkronu iki yönlü).
- `external` öğünde plan beklentisi yok; `fixed`te bağsızsa yalnız serbest.
- Commit: `feat: diet daily view with plan-vs-actual and water amount`

### T15.2 BESİNLER
- Liste ekranı: arama + tür çipleri + **sıralama** (A–Z, kalori ↑↓,
  protein ↓, sık yenen — bellekte). Satır: ad + porsiyon kalorisi +
  100g. Yasaklı rozeti (§5.4 eşleşme: ad katlaması + foodIds bağı).
  Dokunuş → öğün sor → porsiyon sayfası.
- Commit: `feat: browsable food list with sorting and forbidden badges`

### T15.3 Yasaklılar
- `PlanRules.forbidden` çift biçim okuma (`String` | `{label,foodIds}`);
  yazma yeni biçim; AI sözleşmesi string kalır. Editör ekranı: satır
  ekle/sil + "besinlere bağla" çoklu seçim. Daha + Diyet'ten giriş.
- Commit: `feat: forbidden foods editor with food linking`

### T15.4 GEÇMİŞ
- `CalorieWeekChart` + gün dökümü taşındı (M13'te iskelet); gün satırı
  `DayScreen` açar. Kayıtsız gün `—`. Gün satırı tonu
  `resolveCalorieTone` ile (fonksiyon + testi nutrition domain'ine
  taşınır; T16.1 takvim çağrısını söker).
- T13.2'de iskelet zaten kurulduysa bu görev T15.1 ile birleştirilebilir.
- Commit: `feat: diet history tab`

---

## M16 — Spor

### T16.1 PLAN sadeleşmesi
- Takvim hücresi: tip etiketi (SALON/EV/DİNLEN) + ✓/✗; kalori tonu/farkı
  kaldırılır. `resolveCalorieTone`un tek kullanıcısı takvim — çağrı
  çıkınca fonksiyon **T15.4'e taşınır** (Diyet gün dökümü satır tonu
  olarak kullanır); taşınana kadar takvimde kalır, T16.1'de silinir.
  Üst şerit: uyum % (saf: `plan_adherence.dart`).
- Hücre → Planlanan/Yapılan ekranı.
- Commit: `feat: sport-only plan calendar with adherence`

### T16.2 Planlanan/Yapılan ekranı
- İki sütun tablo (hareket · plan · yapılan); satır durumları (✓/amber/
  EKLE). Set düzenleme sheet'i **tarihli** çalışır: geçmiş güne
  `exercise_logs` upsert/sil; seans satırı düzenlenebilir (geçmiş için
  elle `workout_sessions` başlangıç/bitiş). Canlı sayaç yalnız bugün.
- Giriş noktaları: takvim hücresi, Ana Sayfa akış antrenman satırı,
  ANTRENMAN geçmiş listesi.
- Commit: `feat: planned-vs-done screen with past-day set editing`

### T16.3 ANTRENMAN alt sekmesi
- Geçmiş seans listesi (tarih, süre, ≈kcal, hareket sayısı) + aktif seans
  girişi. Aktif ekran değişmez.
- Commit: `feat: workout history tab`

### T16.4 Katalog iki dilli içerik
- Boru hattı: `detailJson` anahtarları `Tr/En` sonekli; mevcut override
  içeriği betikle `Tr`ye taşınır; kaynak `instructions`→`executionEn`.
  Model: alan çiftleri + `resolve(locale)` yardımcıları. Detay ekranı:
  tam içerik (hazırlık/adımlar/nefes/tempo/güvenlik satırları) + kısıt
  eşleşmesinde amber güvenlik. Seed test kademeli çıta (TR zorunlu
  çekirdek, EN `--check` raporu).
- Katalog yer seçimi çipe döner (segment üstte alt sekme olduğundan).
- Commit: `feat: bilingual catalog content and full detail screen`

---

## M17 — Sağlık

### T17.1 TAHLİL yenileme
- VKİ satırı (kilo+boy canlı; eşikler saf `bmi.dart` + test).
- Başlıkta paylaş: son tahlil özeti düz metin olarak `share_plus` ile
  dışa (doktora) — değer + birim + hedef aralık + tarih.
- Değer satırı aralık çubuğu bileşeni (`AppRangeBar`): min/max/hedef
  bandı + değer imleci; renk + konum + metin birlikte.
- Panel özet çipleri.
- Commit: `feat: lab values with range bars and BMI`

### T17.2 Check-up rehberi
- Saf `checkup_rules.dart`: kural tablosu (spec §7.2) + motor
  (yaş, conditionId'ler, VKİ?, son tahlil tarihleri → öneriler).
  Kaynak URL'leri yorumda. Bölüm UI: vakti-geldi/`N` ay sonra; satır
  dokunuşu `lab_schedules`a vade ekler. Kilo/boy eksikse bilgilendirme
  satırı.
- Commit: `feat: checkup guide engine and section`

### T17.3 İLAÇ yenileme
- Sıradaki doz vurgu çizgisi; **7 gün uyum şeridi** (saf hesap + test:
  alınan/planlanan gün başına). Tanımlı listede tür ikonları (reçeteli
  `t-danger`, takviye `t-med`).
- Commit: `feat: medication adherence strip`

### T17.4 Tahlil AI aktarımı
- `lab_import_doc_builder.dart`: `tahlil-aktar.md` (id sözlüğü, JSON
  şeması, birim kuralları, uydurma yasağı).
- Akış ekranı: 3 adım + belgeyi kopyala; yapıştırma → `JsonReader`
  ayrıştırma → önizleme (✓/amber şüpheli: id yok/birim değil/aralık
  10×) → düzeltme → "N kaydet · M atla". `lab_results` yazımı.
- Testler: builder çıktısı, ayrıştırma hataları yapıştırılabilir,
  şüpheli sınıflandırma, onaysız yazım yok.
- Commit: `feat: AI-assisted lab import flow`

---

## M18 — AI köprüsü v2 + süpürme

### T18.1 context.md v2
- Builder yeni bölümler (spec §9.3, 10 bölüm): medikal+tahlil+ilaç
  (sınır satırıyla), ortam+sevilen sporlar, düzen+öğün davranışı,
  yasaklılar, son 14 gün (öğün/su ml/ilaç uyumu/antrenman/kilo),
  besin listesi. Portlar genişler (`ai_bridge/domain/ports.dart` —
  tüketen tanımlar, feature'lar uygular).
- Gönderilecekler ekranı: bölüm anahtarları; kapalı bölüm belgeye
  girmez; "önce belgeyi gör" önizleme (salt-okunur, monospace).
- Commit: `feat: context.md v2 with full data sections`

### T18.2 Kapsam + aşılama
- Plan isteği ekranı: tarih seçici / baştan. Belgeye kapsam talimatı.
- Importer aşılama: korunan günler + değiştirilen kuyruk + hafta
  uzlaşması (spec §9.1); testler: kayıtlara dokunulmaz, weekIndex
  yeniden atanır, sourceRaw eklenir.
- Commit: `feat: graft plan import from a chosen date`

### T18.3 İçe alma kontrolleri
- Kapı 3 ekleri: yasaklı kesişimi, `canPerform`, öğün davranışı
  (`external` doluysa / `fixed` farklıysa), medikal kısıt eşleşmesi.
  Uyarılar amber; reddetme yok. JSON sözleşmesi: öğün `items` desteği +
  doğrulaması (bilinmeyen foodId uyarı).
- Commit: `feat: import warnings - forbidden, equipment, behavior, restrictions`

### T18.4 Süpürme + dokümantasyon
- Ölü kod: eski sekme kalıntıları, taşınan ekran artıkları,
  kullanılmayan ARB anahtarları (grep). `hardcoded_text_test` yeşil.
- CLAUDE.md: v3 mimarisi (5 sekme, yeni feature'lar, v15, iki belge
  köprüsü); spec sapmaları işlenir; test sayısı güncellenir.
- Tam süit + analiz.
- Commit: `docs: record v3 architecture`

---

## Riskler / kararlar
- **En büyük taşıma T13.2** — testlerin çoğu ekran konumuna değil widget'a
  bağlı olduğundan kırılma sınırlı beklenir; kırılanlar davranış değil
  konum güncellemesidir.
- Phosphor paketi sorun çıkarırsa (sürüm çakışması) Material ikonlarla
  devam edilir, ikon değişimi bağımsız commit'e ayrılır — akış durmaz.
- `plan_meal_items` AI sözleşme değişikliği eski planları etkilemez
  (`items` isteğe bağlı).
- Zaman daralırsa fedakârlık sırası (sondan): T16.4 EN içerik betiği
  (yalnız şema hazırlanır), T17.2 UI (motor + test kalır), akış satırı
  sheet'lerinden not/kural (tamamı görünümünde kalır).
