# di@sport M1 — Proje İskeleti Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Boş ama gezilebilir Flutter uygulaması: 5 sekmeli gezinme, tema, Drift veritabanı ayakta, ortak sütun mixin'i ve `Result<T>` tipi hazır, lint kuralları aktif.

**Architecture:** Feature-first (spec Bölüm 4). Bu kilometre taşında `app/` ve `core/` kurulur; `features/` altına yalnızca beş sekmenin yer tutucu ekranları konur. Gezinme go_router olmadan, `NavigationBar` + `IndexedStack` ile yapılır — öğrenme yükünü düşük tutmak bilinçli tercihtir; derin bağlantı gerektiğinde (M5, bildirime dokunma) router eklenir.

**Tech Stack:** Flutter 3.44 / Dart 3.10+, flutter_riverpod ^3.4.0, drift ^2.30.0, drift_flutter ^0.3.1, drift_dev, build_runner, flutter_lints.

**Spec:** `docs/superpowers/specs/2026-08-28-disport-tasarim.md`

## Global Constraints

- Uygulama adı: `disport`, org: `com.kaan` (bundle id `com.kaan.disport`)
- Her tabloda ortak sütunlar: `id TEXT (uuid)`, `userId TEXT` (v1'de `'local'`), `updatedAt INTEGER`, `deletedAt INTEGER?` (spec 5)
- `core` hiçbir feature'ı import edemez (spec 4.3)
- Testler `test/` altında ayna yapıda (spec 4.5)
- freezed/json_serializable bu kilometre taşında YOK — yalnızca `ai_bridge`'de kullanılacak (spec 4.6)
- Commit mesajları İngilizce, conventional commits (`feat:`, `test:`, `chore:`)
- Her görev sonunda `flutter analyze` temiz ve tüm testler yeşil olmalı

**Ortam notu:** Windows'ta geliştirilir, hedef Android. Flutter SDK kurulu DEĞİL — Task 1 kurulumu içerir. Proje dizini `c:\Source\Kaan\di@sport` henüz git deposu değil — Task 2 `git init` içerir.

---

### Task 1: Flutter SDK kurulumu ve doğrulama

**Files:** — (ortam kurulumu)

**Interfaces:**
- Produces: çalışan `flutter` komutu; sonraki tüm görevler buna dayanır

- [ ] **Step 1: Flutter SDK'yı indir ve aç**

PowerShell:

```powershell
New-Item -ItemType Directory -Force C:\dev
# Güncel stable'ı https://docs.flutter.dev/install/archive adresinden alın (3.44.x, Windows zip)
# İndirilen zip'i C:\dev\flutter olarak açın; sonuç: C:\dev\flutter\bin\flutter.bat mevcut
```

- [ ] **Step 2: PATH'e ekle**

```powershell
[Environment]::SetEnvironmentVariable('Path', $env:Path + ';C:\dev\flutter\bin', 'User')
# Yeni terminal açın
```

- [ ] **Step 3: Doğrula**

Run: `flutter --version`
Expected: `Flutter 3.44.x • channel stable`

- [ ] **Step 4: flutter doctor**

Run: `flutter doctor`
Expected: Flutter ve Android toolchain satırları yeşil. Android Studio / Android SDK eksikse `flutter doctor` çıktısındaki yönergeyle kurun (Android Studio → SDK Manager → Android SDK + cmdline-tools; ardından `flutter doctor --android-licenses`). Chrome ve Visual Studio uyarıları önemsizdir — hedefimiz Android.

### Task 2: Proje oluşturma ve git

**Files:**
- Create: proje iskeleti (`flutter create` üretir)
- Create: `.gitignore` (flutter create üretir), `analysis_options.yaml` (değiştirilecek)

**Interfaces:**
- Produces: derlenen boş uygulama; git deposu; tüm sonraki görevler bu ağaçta çalışır

- [ ] **Step 1: Uygulamayı oluştur**

`di@sport` dizini `flutter create` için geçersiz ad içerir (`@`). Uygulama alt dizinde yaşar:

```powershell
cd C:\Source\Kaan\di@sport
flutter create --org com.kaan --project-name disport --platforms android,ios app
```

Sonuç: `C:\Source\Kaan\di@sport\app\` altında proje. Bundan sonraki tüm yollar `app/` köküne görelidir.

- [ ] **Step 2: Çalıştığını doğrula**

```powershell
cd app
flutter test
```

Expected: `flutter create`'in ürettiği örnek widget testi PASS.

- [ ] **Step 3: git init + ilk commit**

```powershell
cd C:\Source\Kaan\di@sport
git init
git add .
git commit -m "chore: flutter scaffold + design docs"
```

(`docs/` ve `kaan-eylul-2026-cizelge.pdf` de depoya girer — spec planla birlikte yaşar.)

### Task 3: Bağımlılıklar ve lint

**Files:**
- Modify: `app/pubspec.yaml`
- Modify: `app/analysis_options.yaml`

**Interfaces:**
- Produces: `flutter_riverpod`, `drift`, `drift_flutter`, `uuid` import edilebilir; `drift_dev`/`build_runner` codegen hazır

- [ ] **Step 1: Bağımlılıkları ekle**

```powershell
cd C:\Source\Kaan\di@sport\app
flutter pub add flutter_riverpod:^3.4.0 drift:^2.30.0 drift_flutter:^0.3.1 uuid:^4.0.0
flutter pub add --dev drift_dev build_runner
```

- [ ] **Step 2: Lint'i sıkılaştır**

`app/analysis_options.yaml` tamamı:

```yaml
include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

linter:
  rules:
    prefer_single_quotes: true
    always_declare_return_types: true
    unawaited_futures: true
    directives_ordering: true
```

Not: spec 4.3'teki "core → features import yasağı" için `custom_lint` altyapısı M2+'ya ertelendi; M1'de kural kod incelemesiyle korunur. Riverpod codegen (`riverpod_generator`) da ilk state ihtiyacı doğduğunda (M3) eklenecek — YAGNI.

- [ ] **Step 3: Doğrula**

Run: `flutter analyze`
Expected: `No issues found!`

- [ ] **Step 4: Commit**

```powershell
git add app/pubspec.yaml app/pubspec.lock app/analysis_options.yaml
git commit -m "chore: add riverpod, drift, uuid; tighten lints"
```

### Task 4: `core/result` — Result tipi

**Files:**
- Create: `app/lib/core/result/result.dart`
- Test: `app/test/core/result/result_test.dart`

**Interfaces:**
- Produces: `sealed class Result<T>` ile `Ok<T>(T value)` / Err`<T>(Failure failure)`; `class Failure({String message, Object? cause})`. `ai_bridge` doğrulama zinciri (M4) ve repository hataları bunu döndürür.

- [ ] **Step 1: Failing test yaz**

`app/test/core/result/result_test.dart`:

```dart
import 'package:disport/core/result/result.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Ok carries value and reports success', () {
    const Result<int> r = Ok(42);
    expect(r.isOk, isTrue);
    expect(r.valueOrNull, 42);
  });

  test('Err carries failure and no value', () {
    const Result<int> r = Err(Failure(message: 'boom'));
    expect(r.isOk, isFalse);
    expect(r.valueOrNull, isNull);
    expect((r as Err<int>).failure.message, 'boom');
  });

  test('switch destructures both cases', () {
    String describe(Result<int> r) => switch (r) {
          Ok(:final value) => 'ok:$value',
          Err(:final failure) => 'err:${failure.message}',
        };
    expect(describe(const Ok(7)), 'ok:7');
    expect(describe(const Err(Failure(message: 'x'))), 'err:x');
  });
}
```

- [ ] **Step 2: Testin FAIL ettiğini gör**

Run: `flutter test test/core/result/result_test.dart`
Expected: FAIL — `Target of URI doesn't exist`

- [ ] **Step 3: Implementasyon**

`app/lib/core/result/result.dart`:

```dart
/// Hata yönetimi için taşıyıcı tip. Exception fırlatmak yerine
/// başarı/başarısızlık açıkça döndürülür (spec Bölüm 10).
sealed class Result<T> {
  const Result();

  bool get isOk => this is Ok<T>;

  T? get valueOrNull => switch (this) {
        Ok(:final value) => value,
        Err() => null,
      };
}

final class Ok<T> extends Result<T> {
  const Ok(this.value);
  final T value;
}

final class Err<T> extends Result<T> {
  const Err(this.failure);
  final Failure failure;
}

final class Failure {
  const Failure({required this.message, this.cause});
  final String message;
  final Object? cause;
}
```

- [ ] **Step 4: Testin PASS ettiğini gör**

Run: `flutter test test/core/result/result_test.dart`
Expected: 3 test PASS

- [ ] **Step 5: Commit**

```powershell
git add app/lib/core/result app/test/core/result
git commit -m "feat: Result<T> sealed type for error handling"
```

### Task 5: `core/db` — AppDatabase ve ortak sütunlar

**Files:**
- Create: `app/lib/core/db/sync_columns.dart`
- Create: `app/lib/core/db/app_database.dart`
- Create: `app/lib/features/settings/data/profile_table.dart`
- Test: `app/test/core/db/app_database_test.dart`

**Interfaces:**
- Consumes: —
- Produces: `mixin SyncColumns on Table` → `id`, `userId` (default `'local'`), `updatedAt`, `deletedAt` sütunları. `class AppDatabase` (`@DriftDatabase`), `AppDatabase.forTesting(NativeDatabase.memory())` kurucusuyla. `ProfileEntries` tablosu (`key`/`value`). Sonraki her feature tablosu `with SyncColumns` yazar ve `app_database.dart`'taki `tables:` listesine eklenir (spec 4.4).

- [ ] **Step 1: Ortak sütun mixin'i**

`app/lib/core/db/sync_columns.dart`:

```dart
import 'package:drift/drift.dart';

/// Spec Bölüm 5: her tabloda bulunan ortak sütunlar.
/// v1'de işlevsizdirler; faz-2 senkronunun kapısını açık tutarlar.
mixin SyncColumns on Table {
  TextColumn get id => text()();
  TextColumn get userId => text().withDefault(const Constant('local'))();
  IntColumn get updatedAt => integer()();
  IntColumn get deletedAt => integer().nullable()();

  @override
  Set<Column<Object>> get primaryKey => {id};
}
```

- [ ] **Step 2: İlk tablo — profile (settings feature'ına ait, spec 4.4: tablo feature'da yaşar)**

`app/lib/features/settings/data/profile_table.dart`:

```dart
import 'package:drift/drift.dart';

import 'package:disport/core/db/sync_columns.dart';

/// Anahtar-değer profil deposu (spec 5.4): boy, hedef kilo, yaşam tarzı…
class ProfileEntries extends Table with SyncColumns {
  TextColumn get key => text()();
  TextColumn get value => text()();
}
```

Not: feature → core importu serbesttir; tersi yasaktır (spec 4.3). `app_database.dart` bir feature dosyasını import edecek — bu, spec 4.4'ün bilinçli tek istisnasıdır: veritabanı toplayıcıdır, mantık içermez.

- [ ] **Step 3: Veritabanı sınıfı**

`app/lib/core/db/app_database.dart`:

```dart
import 'package:drift/drift.dart';
import 'package:drift_flutter/drift_flutter.dart';

import 'package:disport/features/settings/data/profile_table.dart';

part 'app_database.g.dart';

@DriftDatabase(tables: [ProfileEntries])
class AppDatabase extends _$AppDatabase {
  AppDatabase() : super(driftDatabase(name: 'disport'));

  /// Testlerde bellek içi veritabanı (spec Bölüm 11).
  AppDatabase.forTesting(super.e);

  @override
  int get schemaVersion => 1;
}
```

- [ ] **Step 4: Codegen çalıştır**

Run: `dart run build_runner build --delete-conflicting-outputs`
Expected: `app_database.g.dart` üretilir, hata yok.

- [ ] **Step 5: Failing test yaz**

`app/test/core/db/app_database_test.dart`:

```dart
import 'package:disport/core/db/app_database.dart';
import 'package:drift/drift.dart';
import 'package:drift/native.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  late AppDatabase db;

  setUp(() => db = AppDatabase.forTesting(NativeDatabase.memory()));
  tearDown(() => db.close());

  test('profile entry round-trips with sync columns defaulted', () async {
    await db.into(db.profileEntries).insert(ProfileEntriesCompanion.insert(
          id: 'p1',
          updatedAt: 1000,
          key: 'heightCm',
          value: '184',
        ));

    final row = await db.select(db.profileEntries).getSingle();
    expect(row.key, 'heightCm');
    expect(row.value, '184');
    expect(row.userId, 'local'); // withDefault
    expect(row.deletedAt, isNull);
  });
}
```

- [ ] **Step 6: Test çalıştır**

Run: `flutter test test/core/db/app_database_test.dart`
Expected: PASS (implementasyon Step 3'te bitti; bu test şemanın gerçekten çalıştığını kanıtlar). FAIL ederse codegen çıktısını ve tablo tanımını düzelt.

- [ ] **Step 7: Commit**

```powershell
git add app/lib/core/db app/lib/features/settings app/test/core/db
git commit -m "feat: AppDatabase with SyncColumns mixin and profile table"
```

### Task 6: Tema

**Files:**
- Create: `app/lib/app/theme/app_theme.dart`
- Test: `app/test/app/theme/app_theme_test.dart`

**Interfaces:**
- Produces: `AppTheme.light` / `AppTheme.dark` (`ThemeData`); Task 7 bunları `MaterialApp`'e bağlar.

- [ ] **Step 1: Failing test**

`app/test/app/theme/app_theme_test.dart`:

```dart
import 'package:disport/app/theme/app_theme.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('light and dark themes share seed and use Material 3', () {
    expect(AppTheme.light.useMaterial3, isTrue);
    expect(AppTheme.dark.useMaterial3, isTrue);
    expect(AppTheme.light.brightness, Brightness.light);
    expect(AppTheme.dark.brightness, Brightness.dark);
  });
}
```

- [ ] **Step 2: FAIL doğrula**

Run: `flutter test test/app/theme/app_theme_test.dart`
Expected: FAIL — URI yok

- [ ] **Step 3: Implementasyon**

`app/lib/app/theme/app_theme.dart`:

```dart
import 'package:flutter/material.dart';

/// Uygulama teması. Tek tohum renkten iki mod (spec Bölüm 6).
abstract final class AppTheme {
  static const _seed = Color(0xFF2E7D32); // koyu yeşil — sağlık/spor

  static final ThemeData light = ThemeData(
    useMaterial3: true,
    colorScheme: ColorScheme.fromSeed(seedColor: _seed),
  );

  static final ThemeData dark = ThemeData(
    useMaterial3: true,
    colorScheme: ColorScheme.fromSeed(
      seedColor: _seed,
      brightness: Brightness.dark,
    ),
  );
}
```

- [ ] **Step 4: PASS doğrula**

Run: `flutter test test/app/theme/app_theme_test.dart`
Expected: PASS

- [ ] **Step 5: Commit**

```powershell
git add app/lib/app/theme app/test/app/theme
git commit -m "feat: app theme, light and dark from single seed"
```

### Task 7: Beş sekme, gezinme kabuğu, uygulama girişi

**Files:**
- Create: `app/lib/features/today/presentation/today_screen.dart`
- Create: `app/lib/features/plan/presentation/plan_screen.dart`
- Create: `app/lib/features/progress/presentation/progress_screen.dart`
- Create: `app/lib/features/health/presentation/health_screen.dart`
- Create: `app/lib/features/catalog/presentation/catalog_screen.dart`
- Create: `app/lib/app/app.dart`
- Create: `app/lib/bootstrap.dart`
- Modify: `app/lib/main.dart` (tamamen değiştir)
- Delete: `app/test/widget_test.dart` (flutter create kalıntısı)
- Test: `app/test/app/app_test.dart`

**Interfaces:**
- Consumes: `AppTheme.light` / `AppTheme.dark` (Task 6), `AppDatabase` (Task 5)
- Produces: `class DisportApp extends StatelessWidget` — kök widget; `appDatabaseProvider` (`Provider<AppDatabase>`); beş `*Screen` widget'ı. M2+ görevleri kendi ekranının içini doldurur, kabuğa dokunmaz.

- [ ] **Step 1: Failing widget testi**

`app/test/app/app_test.dart`:

```dart
import 'package:disport/app/app.dart';
import 'package:disport/core/db/app_database.dart';
import 'package:drift/native.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  Widget wrap() => ProviderScope(
        overrides: [
          appDatabaseProvider.overrideWithValue(
            AppDatabase.forTesting(NativeDatabase.memory()),
          ),
        ],
        child: const DisportApp(),
      );

  testWidgets('shows five tabs and starts on Today', (tester) async {
    await tester.pumpWidget(wrap());

    expect(find.text('Bugün'), findsWidgets); // sekme + başlık
    for (final label in ['Plan', 'İlerleme', 'Sağlık', 'Katalog']) {
      expect(find.text(label), findsOneWidget);
    }
  });

  testWidgets('tapping a tab switches screen', (tester) async {
    await tester.pumpWidget(wrap());

    await tester.tap(find.text('Katalog'));
    await tester.pumpAndSettle();

    expect(find.text('Katalog ekranı — M2'), findsOneWidget);
  });
}
```

- [ ] **Step 2: FAIL doğrula**

Run: `flutter test test/app/app_test.dart`
Expected: FAIL — URI yok

- [ ] **Step 3: Yer tutucu ekranlar**

Beş dosya aynı kalıptadır. `app/lib/features/today/presentation/today_screen.dart`:

```dart
import 'package:flutter/material.dart';

class TodayScreen extends StatelessWidget {
  const TodayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Bugün ekranı — M3'));
  }
}
```

Diğer dördü — sınıf adı / metin:
- `plan_screen.dart` → `PlanScreen` / `'Plan ekranı — M3'`
- `progress_screen.dart` → `ProgressScreen` / `'İlerleme ekranı — M5'`
- `health_screen.dart` → `HealthScreen` / `'Sağlık ekranı — M5'`
- `catalog_screen.dart` → `CatalogScreen` / `'Katalog ekranı — M2'`

- [ ] **Step 4: Uygulama kabuğu**

`app/lib/app/app.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import 'package:disport/app/theme/app_theme.dart';
import 'package:disport/core/db/app_database.dart';
import 'package:disport/features/catalog/presentation/catalog_screen.dart';
import 'package:disport/features/health/presentation/health_screen.dart';
import 'package:disport/features/plan/presentation/plan_screen.dart';
import 'package:disport/features/progress/presentation/progress_screen.dart';
import 'package:disport/features/today/presentation/today_screen.dart';

/// Veritabanı tekil örneği. Testler override eder (bkz. app_test.dart).
final appDatabaseProvider = Provider<AppDatabase>((ref) {
  final db = AppDatabase();
  ref.onDispose(db.close);
  return db;
});

class DisportApp extends StatelessWidget {
  const DisportApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'di@sport',
      theme: AppTheme.light,
      darkTheme: AppTheme.dark,
      home: const _Shell(),
    );
  }
}

class _Shell extends StatefulWidget {
  const _Shell();

  @override
  State<_Shell> createState() => _ShellState();
}

class _ShellState extends State<_Shell> {
  var _index = 0;

  static const _tabs = [
    (label: 'Bugün', icon: Icons.today, screen: TodayScreen()),
    (label: 'Plan', icon: Icons.calendar_month, screen: PlanScreen()),
    (label: 'İlerleme', icon: Icons.show_chart, screen: ProgressScreen()),
    (label: 'Sağlık', icon: Icons.favorite_outline, screen: HealthScreen()),
    (label: 'Katalog', icon: Icons.fitness_center, screen: CatalogScreen()),
  ];

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text(_tabs[_index].label)),
      body: IndexedStack(
        index: _index,
        children: [for (final t in _tabs) t.screen as Widget],
      ),
      bottomNavigationBar: NavigationBar(
        selectedIndex: _index,
        onDestinationSelected: (i) => setState(() => _index = i),
        destinations: [
          for (final t in _tabs)
            NavigationDestination(icon: Icon(t.icon), label: t.label),
        ],
      ),
    );
  }
}
```

- [ ] **Step 5: Giriş noktaları**

`app/lib/bootstrap.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import 'package:disport/app/app.dart';

Future<void> bootstrap() async {
  WidgetsFlutterBinding.ensureInitialized();
  runApp(const ProviderScope(child: DisportApp()));
}
```

`app/lib/main.dart` (tamamı):

```dart
import 'package:disport/bootstrap.dart';

void main() => bootstrap();
```

`app/test/widget_test.dart` dosyasını sil (eski `MyApp`'e referans verir, derlemeyi kırar).

- [ ] **Step 6: PASS doğrula + tam denetim**

Run: `flutter test` ve `flutter analyze`
Expected: tüm testler PASS, analyze temiz.

- [ ] **Step 7: Emülatörde çalıştır (görsel doğrulama)**

```powershell
flutter emulators                       # kayıtlı emülatörleri listele
flutter emulators --launch <id>         # yoksa Android Studio > Device Manager'dan oluştur
flutter run
```

Expected: uygulama açılır, beş sekme arası geçiş çalışır, koyu/açık tema sistem ayarına uyar.

- [ ] **Step 8: Commit**

```powershell
git add app/lib app/test
git rm app/test/widget_test.dart
git commit -m "feat: five-tab navigation shell with placeholder screens"
```

---

## Self-Review Notları

- **Spec kapsaması:** M1 tanımı (spec 12): iskelet ✓ (T2), `app/`+`core/` ✓ (T4-T7), Drift şeması ✓ (T5), gezinme ✓ (T7), lint ✓ (T3). `custom_lint` ile import-yasağı zorlaması bilinçli olarak M2+'ya ertelendi ve T3'te not edildi.
- **Tip tutarlılığı:** `AppDatabase.forTesting` T5'te tanımlı, T7 testinde aynı imzayla kullanılıyor. `appDatabaseProvider` T7'de tanımlı ve aynı görevin testinde override ediliyor. `AppTheme.light/dark` T6 → T7 tutarlı.
- **Sıra bağımlılığı:** T4 (Result) bu kilometre taşında tüketilmiyor — M4'ün sözleşmesi olduğu için burada kuruldu; spec 10'a dayanır. Erken ama iki dosyalık maliyeti var, YAGNI ihlali saymadım çünkü spec'in hata-yönetimi sözleşmesi tüm feature'ların ortak dili.
